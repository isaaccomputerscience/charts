apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "isaac-app.name" . }}-content-config
  labels:
    {{- include "isaac-app.labels" . | nindent 4 }}
data:
  content-indices.properties: |
    live={{ .Values.contentRepo.liveSha }}
    latest={{ .Values.contentRepo.latestSha }}
